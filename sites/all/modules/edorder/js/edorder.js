(function ($) {
  Drupal.behaviors.edorder = {
    attach: function (context, settings) {
        //console.log(context);
        /*
      $('.example', context).click(function () {
        $(this).next('ul').toggle('show');
      });
      */
     //$( "#edit-field-order-content-und-0-field-quantity-und-0-value--2" ).spinner();
    }
  };
})(jQuery);